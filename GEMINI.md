# Lean Go エージェントシステム設計

## 1. エージェントの役割

本システムは、ユーザーのレビュー負荷を最小限に抑え、プロセスの摩擦を排除するために最適化された合理的なパイプラインです。

### メインエージェント (コーディネーター)
- **役割**: ルーターおよびユーザーインターフェース。
- **責任**: 
    - **ワークスペースの初期化**: 新しいタスクごとに、`YYYYMMDDHHMMSS-short-description` 形式（例: `20260527153045-user-auth`）で一意のセッションIDを生成し、`mkdir -p` を使用してディレクトリ構造 (`aigen/[session_id]/{docs,reports}`) を事前に作成する。
    - **コンテキストの伝搬**: サブエージェントを呼び出す際、`session_id` と `session_dir` を明示的に提供する。
    - **ルーティング (実装)**: ユーザーの要求を Go Implementer に伝え、その際、`base_branch`（ユーザー指定がない場合はデフォルトで `main`）を明示的に指定する。
    - **ルーティング (レビュー)**: 実装結果を Go Reviewer に伝え、`base_branch` (通常は `main`) と `feature_branch` (`feat/[session_id]`) を提供する。
    - **レポートの提示**: 物理ファイル `review_report.md` を読み取り、ユーザーがファイルを開くことなく判断できるよう、その内容（または簡潔な要約）をチャット上に直接提示する。
    - **修正のトリガー**: ユーザーのフィードバックに基づき、再修正を指示する。
    - **最終確定**: ユーザーの承認後、`feature_branch` を `base_branch` にマージし、ワークスペースをクリーンに保つため `feature_branch` を削除する。

### Go Implementer (計画と実装)
- **役割**: 機能デリバリーのエンドツーエンド担当。
- **責任**: 
    - **計画**: Markdown 形式で簡潔な実装計画書 (`docs/plan.md`) を作成する。
    - **実行**: 専用ブランチにてコードとユニットテストを実装する。
    - **検証**: ローカルでテストを実行し、基本的な正しさを保証する。
    - **完了**: 変更をコミットし、メインエージェントに通知する。

### Go Reviewer (品質保証)
- **役割**: 客観的なバリデーター。
- **責任**: 
    - **分析**: `git diff` と `docs/plan.md` を突き合わせて比較する。
    - **評価**: バグ、Idiomatic Go 違反、または要件の漏れを特定する。
    - **報告**: 構造化された Markdown レポートをメインエージェント（およびユーザー）に提供する。
    - **判定**: `PASS` または `FAIL` のバイナリ判定を下す。

---

## 2. Lean ワークフロー

本プロセスは、迅速なデリバリーと高品質な検証に焦点を当てたシンプルな線形ループです。

1. **要求**: `ユーザー` -> `メインエージェント` -> `Go Implementer`
2. **計画と実装**: `Go Implementer`
    - `docs/plan.md`（目的、ロジック、テストケース）を記述。
    - コードとテストを実装 -> `git commit`。
    - メインエージェントに完了を通知。
3. **レビュー**: `メインエージェント` -> `Go Reviewer`
    - Reviewer が `docs/plan.md` と `git diff` を読み取る。
    - Reviewer が **レビューレポート (Markdown)** を生成する。
4. **判断**: `メインエージェント` -> `ユーザー`
    - ユーザーが **レビューレポート** を確認する。
    - **PASS の場合**: ユーザーが承認 -> `メインエージェント` がブランチをマージ。
    - **FAIL/修正の場合**: ユーザーがフィードバックを提供 -> `メインエージェント` -> `Go Implementer`。

---

## 3. 成果物の永続化 (Artifact Persistence)

コンテキストの連続性を確保し、「成果物の消失（ghosting）」を防ぎ、クリーンなワークスペースを維持するため、すべての重要な情報は `aigen/` 配下のセッション専用ディレクトリに保存されます。

### ディレクトリ構造
メインエージェントがタスクごとに一意のセッションIDを決定します。すべての成果物は以下のように保存されます：
`aigen/[session_id]/`
├── `docs/`       # 計画書や要件定義書 (例: `plan.md`)
└── `reports/`    # レビューおよび検証レポート (例: `review_report.md`)

### 主要な成果物
- **`aigen/[session_id]/docs/plan.md`**: 実装内容に関する「唯一の正解（Single Source of Truth）」。
- **Git コミット**: 実際に何が変更されたかを示す正解。
- **`aigen/[session_id]/reports/review_report.md`**: ユーザーが判断材料とする、指摘事項をまとめた Markdown サマリー。

---

## 4. レビューレポート形式 (Markdown)

Reviewer は以下の形式でレポートを提供する必要があります：

# レビューレポート: [タスク名]
**判定:** `PASS` / `FAIL`

### 要件への整合性
- [ ] 要件A: [確認結果]
- [ ] 要件B: [確認結果]

### 技術的な指摘事項
- **[重要度]** [場所]: [問題点] -> [改善案]

### テストカバレッジ
- [ ] 計画されたすべてのテストケースが網羅されているか？
- [ ] エッジケースは考慮されているか？

**要約:** [最終的な簡潔な評価]
